import mysql.connector

class Database:

# Log in to mySQL
def init(self):
mydb = mysql.connector.connect(
  host="localhost",
  user="iferrada",  #yourusername
  password="Vermillion#1", #yourpassword
  database = "TimeStore"
)


mycursor = mydb.cursor()



mycursor.execute("CREATE TABLE times (day int))")


#time inputs
 def storeData(sec, min, hrs, day):
timeStore_day = day
timeStore_hour = hrs
timeStore_min = min
timeStore_sec = sec
mycursor.execute("INSERT INTO times (day, hour, min, sec) VALUES ('{}', {}, '{}')".format(timeStore_hour, timeStore_min, timeStore_sec))

mydb.commit()

#return time columns
def History():
mycursor.execute("SELECT * FROM times")

myresult = mycursor.fetchall()

return myresult
